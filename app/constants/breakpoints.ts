/**
 * レスポンシブデザイン用ブレークポイント定数
 * アプリケーション全体で一貫したブレークポイントを使用するために定義
 */

/** モバイル判定用のメディアクエリ（640px以下） */
export const MOBILE_BREAKPOINT = "(max-width: 640px)";

/** タブレット判定用のメディアクエリ（768px以下） */
export const TABLET_BREAKPOINT = "(max-width: 768px)";

/** デスクトップ判定用のメディアクエリ（1024px以下） */
export const DESKTOP_BREAKPOINT = "(max-width: 1024px)";

